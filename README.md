# hng_stageone
This is stage 1 of hng backend task

# Details
The API allows you insert numbers and get several details like if the number is prime, perfect, odd, eve, or armstrong

# API Endpoint
The endpoint url of my submission was https://hng-stageone-w234.onrender.com/ 

# Setup Instruction
After cloning the repo, install all node modules with "npm i," start the server with "npm run app.js" and Visit localhost:3000
